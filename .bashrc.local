eval "$(/Users/rolentle/hr/bin/hr init -)"
export PATH="$HOME/.rbenv/bin:$PATH"
if which rbenv > /dev/null; then eval "$(rbenv init -)"; fi

alias tml='tmux ls'
alias tmn='tmux new -s'
alias tma='tmux attach -t'
